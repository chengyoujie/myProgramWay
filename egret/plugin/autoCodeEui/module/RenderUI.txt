namespace chm {
    /**
     * AutoCodeEui 生成的界面 
     * euiPath:${path} 
     * made by ${auth}
     * create on ${time} 
    */
    export class ${shortName}RenderUI extends BaseRender {
    
        ${varids}
        
        constructor() {
            super();
        }
    }
}