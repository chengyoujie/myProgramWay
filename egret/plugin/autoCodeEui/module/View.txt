namespace chm {
    /**
     * AutoCodeEui 生成的界面 
     * euiPath:${path} 
     * made by ${auth}
     * create on ${time} 
    */
    export class ${shortName}View extends ${shortName}UI {

        constructor(opt: ShowOptions){
            super(opt);
        }

        /**界面打开**/
        public onOpen() {
            
        }

        /**界面销毁**/
        public destroy(): void {
            super.destroy();
        }
    }
}